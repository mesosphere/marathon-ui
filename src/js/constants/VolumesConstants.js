const VolumesConstants = {
  TYPES: {
    DOCKER: "DOCKER",
    LOCAL: "LOCAL",
    NETWORK: "NETWORK"
  },
  STATUS: {
    ATTACHED: "Attached",
    DETACHED: "Detached"
  }
};

export default Object.freeze(VolumesConstants);
