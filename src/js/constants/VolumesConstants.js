const VolumesConstants = {
  TYPES: {
    DOCKER: "DOCKER",
    LOCAL: "LOCAL"
  },
  STATUS: {
    ATTACHED: "Attached",
    DETACHED: "Detached"
  }
};

export default Object.freeze(VolumesConstants);
