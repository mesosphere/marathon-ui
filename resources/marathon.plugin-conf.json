{
  "plugins": {
    "example": {
      "plugin": "mesosphere.marathon.plugin.http.HttpRequestHandler",
      "implementation": "mesosphere.marathon.example.plugin.http.WebJarHandler",
      "tags": ["ui", "example"],
      "info": {
        "name": "Example Plugin",
        "modules": ["ui"],
        "description": "This is a simple example plugin to demonstrate and test the ui plugins mechanism."
      }
    }
  }
}
